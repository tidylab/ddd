---
title: "Introduction to Domain-driven Design (ddd)"
---

```{r, include = FALSE}
source(file.path(usethis::proj_get(), "R",  "_common.R"))
```


```{r event-storming-table}
event_storming <- 
    tibble::tribble(
        ~Event,                 ~Command,             ~Example,
        "Order issued",         "Start order",        "",
        "Pizza added to order", "Add pizza to order", "",
        "Pizza size selected",  "Select pizza size",  "medium",
        "Topping added",        "Add topping",        "olives",
        "Topping added",        "Add topping",        "anchovy, but only on the right half",
        "Topping removed",      "Remove topping",     "no olives",
        "Pizza reviewed",       "Review pizza",       "",
        "Order confirmed",      "Confirm order",      ""
    )
cache$save(event_storming)
```

