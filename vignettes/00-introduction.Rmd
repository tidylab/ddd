---
title: "Introduction to Domain-driven Design (ddd)"
---

```{r, include = FALSE}
source(file.path(usethis::proj_get(), "R",  "_common.R"))
```


```{r event-storming-table}
event_storming <- 
    tibble::tribble(
        ~Event,                 ~Example,
        "Order issued",         "",
        "Pizza added to order", "",
        "Pizza size decided",   "medium",
        "Topping added",        "olives",
        "Topping added",        "anchovy, but only on the right half",
        "Topping removed",      "no olives",
        "Pizza reviewed",       "",
        "Order confirmed",      ""
    ) %>% 
    tibble::rowid_to_column("#")
cache$save(event_storming)
```

